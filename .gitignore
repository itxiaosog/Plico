# ============================================================
# Plico · Git 提交过滤规则
#
# 判断标准只有一条：**别人 clone 下来能不能自己重新生成它？**
# 能 —— 就不提交（node_modules、构建产物）。
# 不能 —— 必须提交（package-lock.json、Cargo.lock、icons 全套、
#                 Logo-master.png，它们是可复现构建的输入）。
# ============================================================

# ---------- 依赖 ----------
node_modules/

# ---------- 构建产物 ----------
dist/
src-tauri/target/
# Tauri 2 生成的 capability/权限 schema，构建时自动重建
src-tauri/gen/

# ---------- Rust 构建缓存 ----------
*.rs.bk
src-tauri/incremental/

# ---------- 环境变量 / 本地配置 ----------
*.local
.env
.env.*
!.env.example

# ---------- 编辑器 / IDE ----------
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.swo
*~

# ---------- 操作系统 ----------
.DS_Store
Thumbs.db
desktop.ini

# ---------- 日志 ----------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- 临时 / 草稿 ----------
*.tmp
tmp/
temp/

# ============================================================
# 本项目专属：开发过程中的中间产物，一律不入库
# ============================================================

# AI 助手的工作记忆与会话记录。
# 里面有内部设计讨论、本机绝对路径、以及踩坑过程 —— 属于本地上下文，
# 不是项目源码，公开仓库不需要它。
.workbuddy/
.workbuddy-ai/

# CDP 验证脚本用的 Chrome profile 目录。
# 它**不该建在项目里**（会被 Vite 监听导致页面无限重载），脚本已改到
# 系统临时目录；这里兜底防止再被提交进来。
.cdp-*/

# 生成图标时的中间产物（对比用，不入库）。
# 母版 Logo-master.png 要留着 —— 它是可复现的图标源。
.tmp-icons-*/

# ---------- 内部文档（只在本地保留，绝不进入仓库） ----------
# 这三份文档记录了完整的功能规格、视觉规范、以及本机的踩坑过程
# （含本机绝对路径、内部设计取舍），属于开发资料，不适合公开。
# 用 `/` 锚定仓库根目录，精确匹配，不会误伤子目录里的同名文件。
/功能规格说明书.md
/前端视觉规范.md
/开发环境说明.md
